CREATE TABLE IF NOT EXISTS finance_raw.provisional_revenue (
          share STRING,
  date STRING,
  revenue STRING,
  customer_name STRING,
  old_category STRING,
  segment_level_3 STRING,
  category_code STRING,
  revenue_group STRING,
  product_type STRING,
  is_soc STRING,
  product_service_group STRING,
  territory STRING,
  vat_amount STRING,
  account_manager STRING,
  month STRING,
  sales_channel STRING,
  department STRING,
  presale_owner STRING,
  payment_stage STRING,
  gross_amount_including_vat STRING,
  customer_segment STRING,
  payment_request_description STRING,
  customer_group STRING,
  invoice_issue_date STRING,
  revenue_recognition_type STRING,
  revenue_2 STRING,
  share_2 STRING,
  source_file STRING,
  crawled_at_ts BIGINT,
  date_ts BIGINT,
  invoice_issue_date_ts BIGINT
        )
        USING PARQUET
        LOCATION 's3a://vcs-raw/finance-raw/provisional_revenue'
CREATE TABLE IF NOT EXISTS crm_raw.st_users (
          id BIGINT,
  display_name STRING,
  email STRING,
  is_active BOOLEAN,
  work_number STRING,
  mobile_number STRING,
  confirmed BOOLEAN,
  privileges STRING,
  deal_pipeline_id BIGINT,
  job_title STRING,
  language STRING,
  last_login_at STRING,
  time_zone STRING,
  avatar STRING,
  signature STRING,
  email_tracking BOOLEAN,
  access_scope STRING,
  abilities ARRAY<STRING>,
  auto_create_entity BIGINT,
  email_association BOOLEAN,
  reply_to STRING,
  from STRING,
  email_deal_association BOOLEAN,
  freshchat_restore_id STRING,
  is_forgotten BOOLEAN,
  team_ids ARRAY<BIGINT>,
  uuid STRING,
  email_mailbox_ids ARRAY<BIGINT>,
  reports_to_id STRING,
  created_at STRING,
  updated_at STRING,
  role_id STRING,
  user_access_type STRING,
  meta STRUCT<_dummy_:BIGINT>,
  last_login_at_ts BIGINT,
  created_at_ts BIGINT,
  updated_at_ts BIGINT,
  crawled_at_ts BIGINT
        )
        USING PARQUET
        LOCATION 's3a://vcs-raw/crm-raw/st_users'
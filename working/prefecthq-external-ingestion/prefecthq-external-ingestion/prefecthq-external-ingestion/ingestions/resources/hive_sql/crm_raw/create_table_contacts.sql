CREATE TABLE IF NOT EXISTS crm_raw.contacts (
          id BIGINT,
  first_name STRING,
  last_name STRING,
  display_name STRING,
  avatar STRING,
  job_title STRING,
  city STRING,
  state STRING,
  zipcode STRING,
  country STRING,
  email STRING,
  emails ARRAY<STRUCT<id:BIGINT, value:STRING, is_primary:BOOLEAN, label:STRING, _destroy:BOOLEAN>>,
  time_zone STRING,
  work_number STRING,
  mobile_number STRING,
  address STRING,
  last_seen STRING,
  lead_score BIGINT,
  last_contacted STRING,
  open_deals_amount STRING,
  won_deals_amount STRING,
  links STRUCT<conversations:STRING, timeline_feeds:STRING, document_associations:STRING, notes:STRING, tasks:STRING, appointments:STRING, reminders:STRING, duplicates:STRING, connections:STRING>,
  last_contacted_sales_activity_mode STRING,
  custom_field STRUCT<cf_birthday:STRING, cf_old_birthday:STRING, cf_sex:STRING, cf_level_of_interest:STRING, cf_level_of_support:STRING, cf_presales:STRING, cf_partner:BIGINT, cf__new_birthday:STRING, cf__company:STRING, cf__am:STRING, cf__activities:STRING, cf_warm_up_source:STRING, cf_initial_source:STRING, cf_center:STRING, cf_job_title:STRING>,
  created_at STRING,
  updated_at STRING,
  keyword STRING,
  medium STRING,
  last_contacted_mode STRING,
  recent_note STRING,
  won_deals_count BIGINT,
  last_contacted_via_sales_activity STRING,
  completed_sales_sequences STRING,
  active_sales_sequences STRING,
  web_form_ids STRING,
  open_deals_count BIGINT,
  last_assigned_at STRING,
  facebook STRING,
  twitter STRING,
  linkedin STRING,
  is_deleted BOOLEAN,
  team_user_ids ARRAY<BIGINT>,
  external_id STRING,
  work_email STRING,
  subscription_status BIGINT,
  subscription_types STRING,
  unsubscription_reason STRING,
  other_unsubscription_reason STRING,
  customer_fit BIGINT,
  record_type_id STRING,
  whatsapp_subscription_status BIGINT,
  sms_subscription_status BIGINT,
  last_seen_chat STRING,
  first_seen_chat STRING,
  locale STRING,
  total_sessions STRING,
  system_tags ARRAY<STRING>,
  first_campaign STRING,
  first_medium STRING,
  first_source STRING,
  last_campaign STRING,
  last_medium STRING,
  last_source STRING,
  latest_campaign STRING,
  latest_medium STRING,
  latest_source STRING,
  mcr_id BIGINT,
  description STRING,
  amb_subscription_status STRING,
  phone_numbers ARRAY<STRING>,
  tags ARRAY<STRING>,
  sales_account_id BIGINT,
  owner_id BIGINT,
  contact_status_id BIGINT,
  crawled_at_ts BIGINT,
  created_at_ts BIGINT,
  updated_at_ts BIGINT,
  last_assigned_at_ts BIGINT,
  last_contacted_via_sales_activity_ts BIGINT,
  last_contacted_ts BIGINT
        )
        USING PARQUET
        LOCATION 's3a://vcs-raw/crm-raw/contacts'
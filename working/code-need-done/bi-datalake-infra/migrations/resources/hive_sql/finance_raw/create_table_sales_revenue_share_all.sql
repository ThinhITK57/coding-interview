CREATE TABLE IF NOT EXISTS finance_raw.sales_revenue_share_all (
          invoice_date STRING,
  invoice_month STRING,
  invoice_type STRING,
  billing_description STRING,
  revenue_amount STRING,
  revenue_share_amount DOUBLE,
  final_revenue_amount STRING,
  vat_amount STRING,
  gross_amount STRING,
  revenue_recognition_type STRING,
  service_category STRING,
  customer_scope STRING,
  customer_type STRING,
  customer_channel STRING,
  customer_name STRING,
  customer_segment STRING,
  department STRING,
  customer_group STRING,
  account_manager STRING,
  presale DOUBLE,
  service_name STRING,
  revenue_type STRING,
  soc_type STRING,
  mss_shared_revenue STRING,
  market_scope STRING,
  service_code STRING,
  region STRING,
  note DOUBLE,
  current_account_manager STRING,
  is_vvip_customer DOUBLE,
  is_master_contract DOUBLE,
  note__af STRING,
  source_file STRING,
  note__ac STRING,
  crawled_at_ts BIGINT,
  invoice_date_ts BIGINT
        )
        USING PARQUET
        LOCATION 's3a://vcs-raw/finance-raw/sales_revenue_share_all'
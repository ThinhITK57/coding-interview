version: "3.9"
# docker network create  authelia-proxy-internal
networks:
  # authelia-proxy-internal:
  #   external: true
  workflow_authelia-proxy-internal:
    external: true

# ================= COMMON CONFIG =================
x-common-service: &common-service
  # docker build -f Dockerfile.minio -t flow-external-ingestion-minio:latest .
  image: flow-external-ingestion-minio:latest
  restart: always
  volumes:
    - .:/app
  env_file: .env.trino
  environment:
    PREFECT_API_URL: http://prefect-server:4200/api
    HTTP_PROXY: 
    HTTPS_PROXY: 
    http_proxy: 
    https_proxy: 
  extra_hosts: &common-hosts
    # - "prefect-server:10.255.245.150"
    - "datalake.viettelcyber.com:10.255.244.100"
    - "datalake-s3.viettelcyber.com:10.255.244.100"
    - "nocodb.viettelcyber.com:10.255.244.100"
    - "trino.viettelcyber.com:10.255.244.100"
    - "secagi.viettelcyber.com:10.255.244.100"
  networks:
    - workflow_authelia-proxy-internal
  logging:
    driver: "json-file"
    options:
      max-size: "50m"
      max-file: "10"


# ================= SERVICES =================
services:

  trino-clone-table:
    <<: *common-service
    container_name: trino-clone-table
    environment:
      PREFECT_API_URL: http://prefect-server:4200/api
    command: >
      bash -c "
        python trino_clone_table_flow.py
      "

CREATE TABLE IF NOT EXISTS crm_raw.revenue_reconciliation (
          invoice_date STRING,
  report_month STRING,
  billing_type STRING,
  business_center STRING,
  am_segment STRING,
  transaction_description STRING,
  revenue_amount STRING,
  shared_revenue_amount STRING,
  actual_revenue_amount STRING,
  vat_amount STRING,
  gross_amount STRING,
  revenue_type STRING,
  service_category STRING,
  customer_segment_l1 STRING,
  customer_classification STRING,
  customer_channel STRING,
  customer_name STRING,
  segment STRING,
  department_name STRING,
  customer_group STRING,
  account_manager_name STRING,
  presale_name STRING,
  service_offering_name STRING,
  revenue_classification STRING,
  soc_classification STRING,
  shared_revenue_from_mss_amount STRING,
  market_segment STRING,
  service_offering_code STRING,
  source_file STRING,
  vvip_segment STRING,
  channel_name STRING,
  deal_id STRING,
  customer_alias STRING,
  customer_id STRING,
  customer_segment_l2 STRING,
  customer_segment_l3 STRING,
  deployment_type STRING,
  parent_product_category STRING,
  partner_name STRING,
  product_category STRING,
  product_category_code STRING,
  product_service_group STRING,
  product_version STRING,
  revenue_frequency_type STRING,
  team_name STRING,
  territory_name STRING,
  is_hold STRING,
  crawled_at_ts BIGINT,
  invoice_date_ts BIGINT
        )
        USING PARQUET
        LOCATION 's3a://vcs-raw/crm-raw/revenue_reconciliation'

        
CREATE TABLE IF NOT EXISTS crm_raw.revenue_reconciliation (
          invoice_date STRING,
  report_month STRING,
  billing_type STRING,
  business_center STRING,
  am_segment STRING,
  transaction_description STRING,
  revenue_amount STRING,
  shared_revenue_amount STRING,
  actual_revenue_amount STRING,
  vat_amount STRING,
  gross_amount STRING,
  revenue_type STRING,
  service_category STRING,
  customer_segment_l1 STRING,
  customer_classification STRING,
  customer_channel STRING,
  customer_name STRING,
  segment STRING,
  department_name STRING,
  customer_group STRING,
  account_manager_name STRING,
  presale_name STRING,
  service_offering_name STRING,
  revenue_classification STRING,
  soc_classification STRING,
  shared_revenue_from_mss_amount STRING,
  market_segment STRING,
  service_offering_code STRING,
  source_file STRING,
  vvip_segment STRING,
  channel_name STRING,
  deal_id STRING,
  customer_alias STRING,
  customer_id STRING,
  customer_segment_l2 STRING,
  customer_segment_l3 STRING,
  deployment_type STRING,
  parent_product_category STRING,
  partner_name STRING,
  product_category STRING,
  product_category_code STRING,
  product_service_group STRING,
  product_version STRING,
  revenue_frequency_type STRING,
  team_name STRING,
  territory_name STRING,
  is_hold STRING,
  crawled_at_ts BIGINT,
  invoice_date_ts BIGINT
        )
        USING PARQUET
        LOCATION '/opt/datasets/crawlers/vcs_raw/crm-raw/data/revenue_reconciliation'

        
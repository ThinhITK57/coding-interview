name: 'sys_monitor'
version: '1.0.0'
config-version: 2

profile: 'sys_monitor'

model-paths: ['models']
target-path: 'target'
clean-targets: ['target', 'dbt_packages', 'logs']

models:
  sys_monitor:
    staging:
      +materialized: view
    marts:
      +materialized: view

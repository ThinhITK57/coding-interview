version: 2
sources:
- name: pbi_silver__production_management_1
  database: hive
  schema: "{{ env_var('DBT_TRINO_SCHEMA', 'pbi_silver') }}"
  description: "Silver layer sources for domain: production_management_1"
  tables:
  - name: production_management_1_bucket_revision
    description: "Nguồn Power BI: Bucket Revision"
    columns:
    - name: bucket
      description: "Cột Bucket"
